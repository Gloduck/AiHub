# script

+ 可以通过--help来获取脚本的相关参数，非必要不要读取整个脚本。

## 安装环境脚本

- `install_env.sh`: Linux 安装 node、maven、java、python
- `install_env.bat`: Windows 安装 node、maven、java、python
- `install_env_sources.json`: 安装包下载源
